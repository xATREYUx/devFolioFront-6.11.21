import React from "react";

const HomePage = () => {
  return (
    <>
      <div>Home Page</div>
      {/* {posts.map((post) => {
        console.log(post.title);
        return <div>{post.title}</div>;
      })} */}
    </>
  );
};

export default HomePage;
